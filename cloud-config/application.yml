spring:
  profiles:
    active:
    - dev

---
spring:
  profiles: dev    # 开发环境
  application:
    name: cloud-config-dev
    
---
spring:
  profiles: test   # 开发环境
  application:
    name: cloud-config-test

# 请保存为 UTF-8 格式